<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% tasks.each do |task| %>
      <tr>
        <td><%= task.title %></td>
        <td><%= link_to "Show", task_path(task), class: "btn btn-primary btn-sm" %></td>  
      </tr>
    <% end %>
  </tbody>
</table>